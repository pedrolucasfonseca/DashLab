services:
  backend:
    build: ./BackEnd
    container_name: dashlab-backend
    ports:
    - "3001:3001"
    environment:
    - PORT=3001
    - NODE_ENV=production

  frontend:
    build: ./FrontEnd
    container_name: dashlab-frontend
    ports:
    - "80:80"
    depends_on:
      - backend

